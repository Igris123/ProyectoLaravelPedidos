# Proyecto Pedidos Faad Express

### Installation
* Clonar repositorio
* Configurar .env (modificar DB)
* Comandos:
`composer install`\
`php artisan`\
`php artisan key:generate`\
`php artisan migrate`\
`npm install`\
`npm run build`\
`php artisan migrate`\

